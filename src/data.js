/*
const node = {
  id: string,
  url: string,
  timestamp: number,
  openerTabId: number | null,
  thumbnail?: string,

  // computed layout
  x: number,
  y: number
}

const link = {
  source: tabId,
  target: tabId
}
*/
